version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/dev-together
permissions:
  - object: /home/ubuntu/dev-together
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu
